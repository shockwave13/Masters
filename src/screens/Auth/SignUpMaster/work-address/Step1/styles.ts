import EStyleSheet from 'react-native-extended-stylesheet';

const styles = EStyleSheet.create({
  searchInputContainer: {
    marginHorizontal: '$appHeight.$h35',
    marginBottom: 50,
  },
  containerStyle: {
    height: '$appHeight.$h40',
    flexDirection: 'row',
  },
});

export default styles;
